<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="timefish.com.rings4w.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="Rings"
        android:textSize="30sp"
        android:textStyle="bold"
        tools:visibility="gone" />


    <com.victor.ringbutton.RingButton
        android:id="@+id/ringButton"
        android:layout_width="240dp"
        android:layout_height="240dp"
        android:layout_centerInParent="true"
        app:ringbutton_ringColor="@color/colorPrimaryDark"
        app:ringbutton_circleColor="@color/colorPrimary"
        app:ringbutton_downDrawable="@drawable/ic_gallery"
        app:ringbutton_downText="@string/gallery"
        app:ringbutton_textColor="@android:color/white"
        app:ringbutton_upDrawable="@drawable/ic_camera"
        app:ringbutton_upText="@string/take_photo" />
</RelativeLayout>
